# STM32 环境监测网关项目 — 代码分析报告

> 本文档对项目全部源代码进行逐模块、逐函数的分析，供 GPT 了解完整项目上下文。

---

## 一、项目概述

基于 STM32F103RC 的环境监测网关系统，核心功能包括：

- BMP180 传感器采集温度、气压、海拔数据
- LCD 屏幕显示主界面（环境参数 + 天气）和阈值设置界面
- 按键控制界面切换和阈值调节
- 超阈值报警（LED 指示）
- Air780E 4G 模块通过 MQTT 协议上传数据到 OneNET 云平台
- HTTP 协议获取心知天气 API 数据
- RS485 + Modbus RTU 协议作为从机响应上位机查询

---

## 二、硬件平台与引脚映射

| 外设 | 引脚 | 说明 |
|------|------|------|
| LCD (ST7735S) | PB5(CS) PB6(DC) PB7(RES) PB8(MOSI/SDA) PB9(SCLK) | 模拟 SPI 驱动，128x160 |
| BMP180 | PC6(SCL) PC7(SDA) | 软件模拟 I2C，地址 0x77 |
| 按键 KEY | PC1(LEFT) PC4(DOWN) PC5(RIGHT) PA0(WKUP) | LEFT/DOWN/RIGHT 上拉输入，WKUP 下拉输入 |
| LED | PA6(LED1) PA7(LED2) | 推挽输出，高电平灭/低电平亮 |
| 蜂鸣器 BEEP | PC9 | 推挽输出 |
| 继电器 RELAY | PC0 | 推挽输出 |
| RS485 | PC8(DE/RE) PC10(TX) PC11(RX) | UART4，9600 波特率，MAX3485 收发控制 |
| USART1 | PA9(TX) PA10(RX) | 调试串口，115200 |
| USART2 | PA2(TX) PA3(RX) | Air780E 4G 模块，115200 |

---

## 三、系统架构分层

```
┌─────────────────────────────────────────────┐
│  main.c  —  系统初始化 + 主循环调度          │
├─────────────────────────────────────────────┤
│  APP 层（应用逻辑）                          │
│  UI / Sensor / Alarm / Max / Weather        │
│  Modbus / RS485                             │
├─────────────────────────────────────────────┤
│  HARDWARE 层（硬件驱动）                     │
│  LCD / BMP180 / IIC / KEY / LED / BEEP      │
│  RELAY / ONENET                             │
├─────────────────────────────────────────────┤
│  SYSTEM 层（系统支持）                       │
│  delay / sys(位带操作) / usart               │
├─────────────────────────────────────────────┤
│  STM32F10x 标准库 + CMSIS                   │
└─────────────────────────────────────────────┘
```

---

## 四、主程序 main.c 分析

### 4.1 全局变量

| 变量 | 类型 | 作用 |
|------|------|------|
| `Sensor_Update_Flag` | volatile u8 | 500ms 传感器更新标志，TIM2 中断置位，主循环清除 |
| `MQTT_Upload_Flag` | volatile u8 | 30s MQTT 上传标志，TIM2 中断置位，主循环清除 |
| `MQTT_Count` | volatile u16 | 30s 计时器，10ms × 3000 = 30s |

### 4.2 TIM2_Init() — 系统节拍定时器

- 配置 TIM2 产生 10ms 周期中断
- 时钟：72MHz / 7200 = 10kHz，再 / 100 = 100Hz = 10ms
- 抢占优先级 2，子优先级 0

### 4.3 TIM2_IRQHandler() — 定时器中断服务函数

每 10ms 执行一次，**只做计时和置标志**，不做实际业务（不在中断里执行 LCD 操作、Modbus 发送、MQTT 发送、delay_ms）：

- `Sensor_Count` 累加到 50（500ms）→ 置 `Sensor_Update_Flag = 1`
- `MQTT_Count` 累加到 3000（30s）→ 置 `MQTT_Upload_Flag = 1`

### 4.4 main() — 主函数

**初始化流程（严格顺序）：**

1. NVIC 优先级分组（Group 2：2 位抢占 + 2 位子优先级）
2. `delay_init()` — 系统延时
3. `usart_init()` — USART1 调试串口
4. `usart2_init()` — USART2 连接 Air780E
5. `IIC_init()` — I2C（必须在 BMP180 之前）
6. `LCD_Init()` — LCD 初始化
7. `key_init()` — 按键
8. `led_init()` — LED
9. `beep_init()` — 蜂鸣器
10. `BMP180_Init()` — BMP180 传感器（依赖 IIC）
11. `Sensor_Init()` — 传感器数据初始化（依赖 BMP180）
12. `RS485_Init()` — RS485 硬件
13. `Modbus_Init()` — Modbus 协议栈（依赖 RS485）
14. `Alarm_Init()` — 报警初始化
15. `UI_Init()` — UI 初始化
16. `TIM2_Init()` — 最后启动定时器（所有外设就绪后才开始产生中断）
17. `Sensor_Update()` — 立即第一次读取传感器
18. `Modbus_UpdateRegisters()` — 将数据写入 Modbus 寄存器
19. `Weather_Get()` — 上电获取一次天气（心知天气 API）
20. `Connect_init()` — 连接 OneNET MQTT，连接成功后立即上传一次温度/气压/海拔，并重置 30s 计时器

**主循环 while(1) 逻辑：**

1. `Modbus_Process()` — 持续处理 RS485 接收到的 Modbus 请求（非阻塞，必须持续运行）
2. 若 `Sensor_Update_Flag` 置位（每 500ms）：
   - 清除标志
   - `Sensor_Update()` — 读取 BMP180 数据
   - `Modbus_UpdateRegisters()` — 更新 Modbus 寄存器
   - `Alarm_Check()` — 报警检测
   - `UI_ShowSensor()` — 刷新 LCD 传感器数据
3. `UI_Show()` — UI 页面显示/切换
4. `UI_Key_Handler()` — 按键处理
5. 若 `MQTT_Upload_Flag` 置位（每 30s）：
   - 清除标志
   - 依次上传温度、气压、海拔到 OneNET

---

## 五、APP 层 — 应用逻辑模块

### 5.1 UI 模块（ui.c / ui.h）— 界面设计与切换

**核心数据结构：**

```c
typedef enum {
    UI_MAIN = 0,    // 主界面
    UI_SETTING      // 阈值设置界面
} UI_STATE;

typedef enum {
    SET_TEMP_MAX = 0,  // 温度阈值
    SET_PRESS_MAX      // 气压阈值
} SETTING_ITEM;
```

**全局变量：**
- `ui_state` — 当前页面状态，初始 UI_MAIN
- `setting_item` — 设置页面当前选中项，初始 SET_TEMP_MAX
- `ui_refresh` — 刷新标志（1 需要重绘，0 不重绘）
- `buf[20]` — 格式化字符串缓冲区

**函数清单：**

| 函数 | 功能 |
|------|------|
| `UI_ShowOneChinese(x, y, s, fc, bc)` | (static) 封装 LCD 显示单个 12x12 汉字 |
| `UI_ShowWeatherText(x, y, text)` | 将英文天气文本转换为中文显示。支持 Sunny→晴、Cloudy→多云、Overcast→阴、Light/Moderate/Heavy rain→小/中/大雨、Thunderstorm→雷雨、Light/Moderate/Heavy snow→小/中/大雪、Fog→雾、Haze→霾、Dust→沙尘、Sleet→冻雨、Zhengzhou→郑州(黄色)、Ice→冰。无匹配时直接显示原始英文 |
| `UI_Init()` | 初始化 UI：状态设为主界面，选中项设为温度阈值，刷新标志置 1 |
| `UI_Show()` | UI 显示管理。若 `ui_refresh==0` 直接返回不重绘；否则根据 `ui_state` 调用 UI_Main() 或 UI_Setting()，完成后清除刷新标志 |
| `UI_ShowSensor()` | 只刷新主界面的传感器数值区域（温度/气压/海拔），不刷新标题和天气。先清除对应区域背景再绘制新值。非主界面时不执行 |
| `UI_Key_Handler()` | 按键处理。WKUP 切换主界面/设置界面；DOWN 在设置界面切换选中项(温度↔气压)；RIGHT 增加阈值(温度+1 上限100，气压+100 上限120000)；LEFT 减少阈值(温度-1 下限0，气压-100 下限80000)。每次操作后置刷新标志 |
| `UI_Main()` | 绘制主界面：清屏→标题"ENV-MON"→分割线→城市(中文)→天气(中文)→室外温度"OUT:"→分割线→温度/气压/海拔标签(汉字"温度""气压""海拔") |
| `UI_Setting()` | 绘制设置界面：清屏→标题"SETTING"→分割线→"TEMP MAX:"+温度阈值(红色)→"PRESS MAX:"+气压阈值(红色)→选择箭头">"(绿色，指示当前选中项) |

**界面布局（主界面，128x160 竖屏）：**
- Y=8: 标题 "ENV-MON"
- Y=25: 分割线
- Y=35: 城市名(中文) + 天气(中文)
- Y=55: "OUT:" + 室外温度
- Y=75: 分割线
- Y=85: 温度数据
- Y=105: 气压数据
- Y=125: 海拔数据

### 5.2 Sensor 模块（sensor.c / sensor.h）— 传感器数据封装

**数据结构：**
```c
typedef struct {
    double temp;       // 温度 ℃
    double pressure;   // 气压 Pa
    double altitude;   // 海拔 m
} Sensor_Data;
```

**全局变量：** `sensor` — 传感器数据实例

**函数：**

| 函数 | 功能 |
|------|------|
| `Sensor_Init()` | 初始化传感器数据为 0 |
| `Sensor_Update()` | 读取 BMP180 数据并计算。温度 = `BMP180_ReadTemperature()/10.0`（BMP180 返回 0.1℃ 精度，除以 10 转为 ℃）；气压 = `BMP180_ReadPressure()`（Pa）；海拔 = `44330*(1-pow(pressure/101325, 0.1903))`（标准大气压公式） |

### 5.3 Alarm 模块（alarm.c / alarm.h）— 阈值报警

**函数：**

| 函数 | 功能 |
|------|------|
| `Alarm_Init()` | 报警初始化，LED1 置高（灭）|
| `Alarm_Check(temp, pressure)` | 阈值检测。注意：虽然接收参数但实际使用 `sensor.temp` 和 `sensor.pressure` 全局变量比较。温度超阈值→LED1 亮(低电平)；气压超阈值→LED2 亮(低电平)；正常则对应 LED 灭 |

### 5.4 Max 模块（max.c / max.h）— 阈值存储

**数据结构：**
```c
typedef struct {
    double temp_max;        // 温度上限
    double pressure_max;    // 气压上限
    double pressure_min;    // 气压下限（已定义但当前未使用）
} MAX_TypeDef;
```

**全局变量：** `max` — 初始化值为 `{28.0, 99999, 90000}`

### 5.5 Weather 模块（weather.c / weather.h）— 心知天气 API

**数据结构：**
```c
typedef struct {
    char city[32];        // 城市名
    char text[32];        // 天气描述(英文)
    char temperature[8];  // 温度
} WeatherData;
```

**全局变量：** `weather` — 天气数据实例

**函数：**

| 函数 | 功能 |
|------|------|
| `Weather_WaitCheck(str, timeout)` | (static) 等待 USART2 缓冲区中出现指定字符串，超时返回 1，成功返回 0 |
| `Weather_Parse()` | (static) 手动 JSON 解析。在 USART2 缓冲区中查找 `{"results"`，依次提取 `name`(城市)、`text`(天气)、`temperature`(温度) 字段。使用 strstr + strchr 手动解析，非完整 JSON 库 |
| `Weather_Get()` | 完整天气获取流程：① 关闭旧 HTTP 会话(AT+HTTPTERM) ② GPRS 配置(AT+SAPBR 设置 CONTYPE 和 APN) ③ 打开数据连接(AT+SAPBR=1,1 和 =2,1) ④ HTTP 初始化(AT+HTTPINIT, AT+HTTPPARA CID=1) ⑤ 设置天气 API URL(心知天气 seniverse.com, 城市 zhengzhou, 英语) ⑥ HTTP GET(AT+HTTPACTION=0, 等待 200 状态码) ⑦ 读取数据(AT+HTTPREAD) ⑧ JSON 解析 ⑨ 释放 HTTP。返回 0 成功 |

### 5.6 Modbus 模块（modbus.c / modbus.h）— Modbus RTU 从机

**宏定义：**
- `MODBUS_SLAVE_ID` = 1（从机地址）
- `MODBUS_REG_NUM` = 10（保持寄存器数量）
- `MODBUS_REQUEST_LEN` = 8（请求帧长度）
- `MODBUS_RESPONSE_LEN` = 24（最大响应长度）

**全局变量：** `Modbus_HoldingReg[10]` — 保持寄存器数组

**函数：**

| 函数 | 功能 |
|------|------|
| `Modbus_Init()` | 初始化保持寄存器为 100,200,...,1000，打印从机信息 |
| `Modbus_UpdateRegisters()` | 将传感器数据写入寄存器。寄存器0=温度×10(保留1位小数)；寄存器1=气压/10(kPa)；寄存器2=海拔×10；寄存器3-9 预留为 0 |
| `Modbus_CRC16(buf, len)` | Modbus RTU CRC16 校验。初始值 0xFFFF，多项式 0xA001，返回低字节在前的 CRC |
| `Modbus_SendException(slave_id, function, exception_code)` | (static) 发送异常响应帧。功能码最高位置 1(0x03→0x83)，附带异常码和 CRC |
| `Modbus_Function03(request)` | (static) 处理功能码 03（读保持寄存器）。解析起始地址和数量，校验 CRC，检查地址范围和数量合法性，构造响应帧（ID+功能码+字节数+数据+CRC），通过 RS485 发送 |
| `Modbus_Process()` | 非阻塞从机处理。① 关闭 UART4 接收中断（防止竞态）② 检查缓冲区数据长度 ③ 在缓冲区中扫描合法帧（匹配 Slave ID + 功能码 03 + CRC）④ 找到合法帧后从缓冲区移除该帧 ⑤ 恢复中断 ⑥ 调用 Modbus_Function03 处理。无数据时立即返回不阻塞主循环 |

### 5.7 RS485 模块（rs485.c / rs485.h）— RS485 硬件驱动

**宏定义：**
- `RS485_TX_EN()` — PC8 置高，进入发送模式
- `RS485_RX_EN()` — PC8 置低，进入接收模式
- `RS485_RX_BUF_SIZE` = 64

**全局变量：**
- `RS485_RX_BUF[64]` — 接收缓冲区（volatile）
- `RS485_RX_LEN` — 接收数据长度（volatile）

**函数：**

| 函数 | 功能 |
|------|------|
| `RS485_Init()` | 初始化 UART4（PC10/TX, PC11/RX, 9600 8N1）。配置 PC8 为 MAX3485 方向控制。默认进入接收模式。开启 UART4 接收中断，抢占优先级 1 |
| `RS485_SendByte(data)` | 发送单个字节（等待 TXE 标志） |
| `RS485_SendData(buf, len)` | 发送数据帧。流程：切换发送模式→逐字节发送→等待 TC（发送完成）→切换回接收模式 |
| `RS485_ReceiveByte()` | 阻塞接收单字节（旧测试函数，Modbus 通信不再使用） |
| `RS485_ReceiveData(buf, max_len)` | 非阻塞接收数据（旧测试函数，Modbus 通信不再使用） |
| `UART4_IRQHandler()` | UART4 中断服务函数。每收到一个字节存入 `RS485_RX_BUF`，长度递增。缓冲区满则重置。中断里只做数据存储，不做 CRC/解析/发送（交给主循环 Modbus_Process） |

---

## 六、HARDWARE 层 — 硬件驱动

### 6.1 LCD 驱动（lcd.c / lcd.h + lcd_init.c / lcd_init.h）

**芯片：** ST7735S，128×160，RGB565，模拟 SPI 通信

**lcd_init.h 关键定义：**
- `USE_HORIZONTAL` = 1（竖屏方向）
- `LCD_W` = 128, `LCD_H` = 160
- 引脚宏：SCLK=PB9, MOSI=PB8, RES=PB7, DC=PB6, CS=PB5
- 颜色宏：WHITE/BLACK/RED/GREEN/BLUE/YELLOW 等 RGB565 值

**lcd_init.c 函数：**

| 函数 | 功能 |
|------|------|
| `LCD_GPIO_Init()` | 初始化 GPIOB 的 PB5-PB9 为推挽输出 |
| `LCD_Writ_Bus(dat)` | 模拟 SPI 写一个字节（CS 拉低→逐位移入 MOSI，SCLK 时序→CS 拉高）|
| `LCD_WR_DATA8(dat)` | 写 8 位数据 |
| `LCD_WR_DATA(dat)` | 写 16 位数据（先高字节后低字节）|
| `LCD_WR_REG(dat)` | 写命令（DC 拉低写命令，再拉高准备写数据）|
| `LCD_Address_Set(x1,y1,x2,y2)` | 设置显示窗口地址。根据横竖屏方向有不同偏移（+1 或 +2）|
| `LCD_Init()` | LCD 完整初始化序列：GPIO 初始化→复位→Sleep Out(0x11)→帧率设置(B1/B2/B3)→Dot inversion(B4)→电源序列(C0/C1/C2/C3/C4)→VCOM(C5)→方向(36h)→Gamma 校正(E0/E1)→65K 色模式(3Ah)→显示开(29h) |

**lcd.c 函数：**

| 函数 | 功能 |
|------|------|
| `LCD_Fill(xsta,ysta,xend,yend,color)` | 指定矩形区域填充颜色 |
| `LCD_DrawPoint(x,y,color)` | 画单个像素点 |
| `LCD_DrawLine(x1,y1,x2,y2,color)` | 画直线（Bresenham 算法）|
| `LCD_DrawRectangle(x1,y1,x2,y2,color)` | 画矩形框（四条线）|
| `Draw_Circle(x0,y0,r,color)` | 画圆（中点画圆法）|
| `LCD_ShowChinese(x,y,s,fc,bc,sizey,mode)` | 显示汉字串，根据字号调用对应函数 |
| `LCD_ShowChinese12x12(x,y,s,fc,bc,sizey,mode)` | 显示单个 12x12 汉字。从 `tfont12` 字库数组中按 GB2312 编码匹配，支持叠加/非叠加模式 |
| `LCD_ShowChinese16x16(...)` | 同上，16x16 字号，字库 `tfont16` |
| `LCD_ShowChinese24x24(...)` | 同上，24x24 字号，字库 `tfont24` |
| `LCD_ShowChinese32x32(...)` | 同上，32x32 字号，字库 `tfont32` |
| `LCD_ShowChar(x,y,num,fc,bc,sizey,mode)` | 显示单个 ASCII 字符。从 `ascii_1206/1608/2412/3216` 字库取模 |
| `LCD_ShowString(x,y,p,fc,bc,sizey,mode)` | 显示 ASCII 字符串（逐字符调用 ShowChar）|
| `mypow(m,n)` | 幂运算（辅助函数）|
| `LCD_ShowIntNum(x,y,num,len,fc,bc,sizey)` | 显示整数（带前导零消除）|
| `LCD_ShowFloatNum1(x,y,num,len,fc,bc,sizey)` | 显示两位小数 |
| `LCD_ShowPicture(x,y,length,width,pic)` | 显示图片（从数组读取 RGB565 数据）|

**字库文件：**
- `lcdfont.h` — 包含 ASCII 字库(ascii_1206/1608/2412/3216)和汉字字库(tfont12/16/24/32)，每个汉字条目含 Index(2字节编码) + Msk(点阵数据)
- `pic.h` — 图片数据数组

### 6.2 BMP180 驱动（bmp180.c / bmp180.h）

**芯片：** Bosch BMP180 气压传感器，I2C 接口，地址 0x77

**bmp180.h 关键定义：**
- 寄存器：CHIP_ID(0xD0), CONTROL(0xF4), RESULT(0xF6)
- 命令：温度测量(0x2E), 气压测量(0x34, OSS=0)
- `BMP180_TypeDef` 结构体：校准参数(AC1-AC6, B1, B2, MB, MC, MD) + 原始数据(UT, UP) + 中间变量(B5) + 结果(temperature, pressure, altitude)

**函数：**

| 函数 | 功能 |
|------|------|
| `BMP180_ReadByte(reg)` | I2C 读一个字节寄存器 |
| `BMP180_WriteByte(reg, data)` | I2C 写一个字节寄存器 |
| `BMP180_Read16(reg)` | I2C 读 16 位寄存器（先高后低），用于校准参数 |
| `BMP180_ReadID()` | 读取芯片 ID（寄存器 0xD0，应为 0x55）|
| `BMP180_ReadCalibration()` | 读取 11 个校准参数(AC1-AC6, B1, B2, MB, MC, MD)，存入结构体 |
| `BMP180_Init()` | 初始化：延时→读 ID→ID==0x55 则读取校准参数 |
| `BMP180_ReadTemperature()` | 读取温度。发送温度测量命令→延时 5ms→读取原始 UT→按 BMP180 数据手册公式计算（X1, X2, B5）→返回 0.1℃ 精度温度 |
| `BMP180_ReadPressure()` | 读取气压。发送气压测量命令(OSS=0)→延时 5ms→读取 19bit 原始 UP→右移 8 位→按数据手册多步补偿公式计算（B3/B4/B6/B7, X1/X2/X3, 二次修正）→返回 Pa |
| `BMP180_ReadAltitude()` | 计算海拔。使用标准大气压公式：`44330*(1-(pressure/101325)^0.1903)` |

### 6.3 IIC 驱动（iic.c / iic.h）— 软件模拟 I2C

- SCL=PC6, SDA=PC7，开漏输出
- `SDA_IN()` / `SDA_OUT()` — 通过修改 GPIOC->CRL 寄存器切换 SDA 方向
- 位带操作：`IIC_SDA = PCout(7)`, `IIC_SCL = PCout(6)`, `READ_SDA = PCin(7)`

**函数：**

| 函数 | 功能 |
|------|------|
| `IIC_init()` | 初始化 PC6/PC7 为开漏输出，总线拉高 |
| `IIC_Start()` | 产生起始信号（SCL 高时 SDA 下降沿）|
| `IIC_Stop()` | 产生停止信号（SCL 高时 SDA 上升沿）|
| `IIC_Ack()` | 发送 ACK 应答 |
| `IIC_NAck()` | 发送 NACK 非应答 |
| `IIC_Wait_Ack()` | 等待从机应答，超时 200 次返回 1（非应答）|
| `IIC_Send_Byte(tx)` | 发送一个字节（MSB 先发）|
| `IIC_Read_Byte(ack)` | 读取一个字节，参数决定发 ACK 还是 NACK |
| `IIC_Scan()` | I2C 总线设备扫描（1~127 地址遍历，有应答则打印地址）|

### 6.4 KEY 驱动（key.c / key.h）— 按键

- DOWN=PC4, LEFT=PC1, RIGHT=PC5（上拉输入，按下为低）
- WKUP=PA0（下拉输入，按下为高）

**函数：**

| 函数 | 功能 |
|------|------|
| `key_init()` | 初始化 GPIOA(PA0 下拉) 和 GPIOC(PC1/PC4/PC5 上拉) |
| `Key_scan(mode)` | 按键扫描。mode=0 不支持连按（松开后才能再次触发），mode=1 支持连按。返回值：0 无按键，1=DOWN，2=LEFT，3=RIGHT，4=WKUP |

### 6.5 LED 驱动（led.c / led.h）

- LED1=PA6, LED2=PA7，推挽输出
- `LED1 = PAout(6)`, `LED2 = PAout(7)` — 位带操作宏
- `led_init()` — 初始化 PA6/PA7 推挽输出，默认高电平（灭）

### 6.6 BEEP 驱动（beep.c / beep.h）

- BEEP=PC9，推挽输出
- `BEEP = PCout(9)` — 位带操作宏
- `beep_init()` — 初始化 PC9 推挽输出，默认低电平（不响）

### 6.7 RELAY 驱动（relay.c / relay.h）

- RELAY=PC0，推挽输出
- `RELAY = PCout(0)` — 位带操作宏
- `relay_init()` — 初始化 PC0 推挽输出，默认低电平（断开）
- 注意：relay 模块已实现但当前 main.c 中未调用 `relay_init()`，也未使用

### 6.8 ONENET 模块（onenet.c / onenet.h）— OneNET 云平台 MQTT

**认证信息：**
- Device_ID = "Ai_car"
- Product_ID = "jDa1z3yUDm"
- Token = OneNET 鉴权令牌（MD5 签名，有效期到 2028715666）

**函数：**

| 函数 | 功能 |
|------|------|
| `Receive_Clear()` | (static) 清空 USART2 缓存 |
| `Wait_Check(str, timeout)` | (static) 等待 USART2 缓冲区出现指定字符串 |
| `Connect_init()` | OneNET MQTT 连接流程：① 关闭旧连接(AT+MIPCLOSE) ② ATE0 关回显 ③ AT+CGREG? 查询网络注册 ④ AT+CGATT? 查询网络附着 ⑤ AT+MCONFIG 配置 MQTT 登录参数(DeviceID/ProductID/Token) ⑥ AT+MIPSTART 连接 mqtts.heclouds.com:1883 ⑦ AT+MCONNECT=1,120 建立 MQTT 连接 ⑧ AT+MSUB 订阅 `$sys/{ProductID}/{DeviceID}/thing/#` 主题。返回 0 成功 |
| `Up_Temp_Data(temp)` | 上传温度。通过 AT+MPUB 发布到 `$sys/{ProductID}/{DeviceID}/thing/property/post` 主题，JSON 格式 `{"id":"123","version":"1.0","params":{"temp":{"value":xx}}}`。\\22 为双引号转义 |
| `Up_Pressure_Data(pressure)` | 上传气压，JSON 字段 `pressure` |
| `Up_Altitude_Data(altitude)` | 上传海拔，JSON 字段 `altitude` |

---

## 七、SYSTEM 层 — 系统支持

### 7.1 sys.h — 位带操作

- 提供 `PAout(n)`/`PAin(n)`/`PBout(n)`/`PBin(n)`/`PCout(n)`/`PCin(n)` 等宏
- 基于 Cortex-M3 位带区，实现类似 51 单片机的单 GPIO 位操作
- 定义 `bool`/`true`/`false`
- 声明汇编函数：WFI_SET, INTX_DISABLE, INTX_ENABLE, MSR_MSP

### 7.2 USART（usart.c / usart.h）— 串口通信

**USART1（调试串口）：**
- PA9(TX) / PA10(RX)，115200 8N1
- `printf` 重定向到 USART1（通过 fputc 重定向 + 关闭半主机模式）
- 接收缓冲区 `USART_RX_BUF[200]`，状态变量 `USART_RX_STA`
- 中断优先级：抢占 3，子 3

**USART2（Air780E 通信）：**
- PA2(TX) / PA3(RX)，115200 8N1
- 接收缓冲区 `USART2_RX_BUF[1024]`，长度变量 `USART2_RX_LEN`
- 中断优先级：抢占 0，子 3（最高优先级，确保 4G 数据不丢失）
- 中断中同时将数据转发到 USART1 用于调试观察

**函数：**

| 函数 | 功能 |
|------|------|
| `usart_init()` | 初始化 USART1 |
| `usart2_init()` | 初始化 USART2 |
| `USART_SendString(USARTx, DataString)` | 通用串口发送字符串 |
| `USART2_SendString(str)` | USART2 发送字符串（封装 USART_SendString）|
| `USART2_Clear()` | 清空 USART2 接收缓存（关中断→memset→清长度→开中断）|

---

## 八、中断优先级分配总览

| 中断源 | 抢占优先级 | 子优先级 | 说明 |
|--------|-----------|---------|------|
| USART2 (Air780E) | 0 | 3 | 最高，确保 4G 数据不丢 |
| UART4 (RS485) | 1 | 1 | Modbus 通信 |
| TIM2 (系统节拍) | 2 | 0 | 10ms 定时 |
| USART1 (调试) | 3 | 3 | 最低 |

---

## 九、五日开发计划完成情况对照

| 计划 | 内容 | 完成情况 |
|------|------|---------|
| Day 1 | LCD 驱动 + 界面设计 + 界面切换 | ✅ 已完成。lcd_init.c 实现 ST7735S 底层驱动；lcd.c 实现字符/汉字/图形显示；ui.c 实现主界面(环境参数+天气)和阈值设置界面，WKUP 键切换 |
| Day 2 | BMP180 驱动 + 阈值交互 | ✅ 已完成。bmp180.c 完成温度/气压/海拔采集；sensor.c 封装数据；max.c 存储阈值；alarm.c 实现 LED 报警；LCD 实时显示传感器数据 |
| Day 3 | 4G 联网 + 远程交互 | ✅ 已完成。onenet.c 实现 MQTT 对接 OneNET(连接+订阅+数据上传)；weather.c 实现 HTTP 对接心知天气 API(含 JSON 解析)；usart.c 实现 USART2 双串口通信 |
| Day 4 | RS485 + Modbus RTU | ✅ 已完成。rs485.c 实现 UART4 硬件驱动(含 MAX3485 方向控制)；modbus.c 实现完整 RTU 从机(CRC16 + 功能码03 + 异常响应 + 非阻塞处理) |
| Day 5 | 项目融合与优化 | ✅ 已完成。main.c 统一调度所有模块；TIM2 10ms 节拍驱动 500ms 传感器更新和 30s MQTT 上传；中断与主循环协作避免阻塞 |

---

## 十、当前代码注意事项与潜在问题

1. **Alarm_Check 参数冗余**：函数接收 `temp` 和 `pressure` 参数，但函数体内实际使用的是全局变量 `sensor.temp` 和 `sensor.pressure`，传入的参数未使用
2. **pressure_min 未使用**：`max` 结构体中定义了 `pressure_min` 字段（初始 90000），但 Alarm_Check 只检测了上限，未检测下限
3. **蜂鸣器未使用**：beep.c 已实现但 main.c 中只调用了 `beep_init()`，报警逻辑中未触发蜂鸣器（只用了 LED）
4. **继电器未使用**：relay.c 已实现但 main.c 中完全未调用
5. **天气只获取一次**：Weather_Get() 仅在 main() 初始化时调用一次，主循环中不定期刷新
6. **Modbus 寄存器1气压单位**：注释说存 kPa(pressure/100)，但代码实际是 `pressure/10.0`，存在注释与代码不一致
7. **UI_ShowSensor 坐标硬编码**：数据刷新区域坐标(45,85,120,96 等)为硬编码，与 UI_Main 中标签位置需手动保持一致
8. **缓冲区安全性**：UI_ShowWeatherText 中直接将城市名"Zhengzhou"作为特殊分支处理显示"郑州"，属于硬编码逻辑

---

*以上为项目全部源代码的完整分析，涵盖 APP 层 7 个模块、HARDWARE 层 8 个驱动、SYSTEM 层 3 个支持模块，共约 20 个源文件。*
